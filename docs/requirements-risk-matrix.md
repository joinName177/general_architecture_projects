# 认证需求与风险矩阵

| 需求/风险  | 控制                                               | 验证                       | 残余风险                   |
| ---------- | -------------------------------------------------- | -------------------------- | -------------------------- |
| 用户注册   | Zod 输入校验、后端唯一索引与稳定冲突码             | 组件测试、API 集成测试     | 尚未邮箱验证               |
| 用户登录   | 通用错误文案、Argon2id、服务端会话                 | Use Case、负向登录测试     | 公网前需加强按 IP/账号限流 |
| 会话保密   | access token 仅内存；refresh token HttpOnly Cookie | Adapter 测试、刷新轮换联调 | XSS 仍可在当前页面调用 API |
| 超级管理员 | Secret 注入、幂等创建、禁止普通用户静默提权        | 启动与数据库联调           | 尚未提供 MFA               |
| 契约漂移   | 固定 OpenAPI SHA-256、生成物零漂移检查             | `pnpm generated:check`     | 前后端仍需协调发布         |
