# AGENTS.md

## 1. 适用范围与唯一性

本文件适用于项目根目录及全部子目录，是本项目唯一的 Agent 行为规范。不得新增 `CLAUDE.md`、`.cursor/rules`、嵌套 `AGENTS.md` 或其他复制本文件内容的工具专属指令。

本文件只定义协作方式，不复制架构、代码或 Git 规则。各类事实的唯一来源如下：

| 主题 | 唯一来源 |
| --- | --- |
| 项目实例事实 | 实例项目的 `architecture-profile.yaml` 及 Schema |
| 架构边界 | 项目选定并实例化的 `ARCHITECTURE.md` |
| 代码与工具规则 | `engineering-standards.md` 和可执行配置 |
| Git、Commit、PR、合并 | `CONTRIBUTING.md` 与仓库保护设置 |
| Agent 工作方式 | 本文件 |

发现规则缺失、冲突或实现与规范不一致时，必须在同一变更中修正规范和实现；不能选择较宽松的一份继续工作。

## 2. 开始任务前

1. 阅读本文件、`CONTRIBUTING.md`、`engineering-standards.md` 和 `ARCHITECTURE.md`。
2. 检查工作区状态，区分用户已有修改与本次任务修改；不得覆盖或清理无关变更。
3. 将需求转化为可验证的完成条件。多步骤任务先给出简短计划，并为每一步指定验证方式。
4. 明确假设。不会改变范围、架构、安全或外部状态的安全假设可以继续；会产生上述影响时必须先确认。
5. 先寻找现有实现、契约和配置，不得因未搜索就创建第二套能力。

## 3. 实施原则

- 只实现需求所需的最小完整方案，不添加无真实调用者的抽象、扩展点、配置项或“以后可能使用”的依赖。
- 修改范围必须可追溯到需求。不要顺带重构无关代码；本次变更产生的废代码、依赖、配置和文档必须同时删除。
- 现有风格只有在符合当前规范时才能沿用；不能复制已知违规。
- 外部 API、身份、IPC、文件、配置和用户输入等不可信边界必须完整验证。不得用“理论上不会发生”省略错误、取消和资源释放路径。
- 严格执行 `engineering-standards.md` 的生成代码、类型、依赖和零债务规则；不得修改或弱化工具配置、测试和 CI 来绕过失败。
- 新增或升级依赖前先按工程规范证明必要性和影响，不因实现便利扩大任务范围。

## 4. 项目边界

- 只实现 Profile 和选定架构授权的客户端范围；不得推断或创建未选择的运行目标、服务端能力、第二实现或技术体系。
- 业务模块、状态、身份、网络、UI、契约和发布规则全部引用选定架构，不在本文件维护另一份清单。
- 不执行破坏性 Git 操作，不改写共享历史，不擅自提交、推送、发布或修改外部系统。

## 5. 验证

验证强度必须与风险相称，但不能只靠阅读代码：

- 文档或 Schema 变更：执行 JSON/Schema 严格校验、交叉引用检查、`git diff --check`；修改 Mermaid 时渲染全部图。
- TypeScript 项目：执行 `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm architecture:check`、测试和 production build；最终以 `pnpm verify` 为准。
- Tauri 项目：除前端门禁外，执行 Rust fmt、Clippy `-D warnings`、测试、供应链检查、原生构建及相应平台测试。
- 契约、生成器、身份、安全、发布或依赖边界变化必须运行对应负向测试，不能只验证成功路径。

如果仓库缺少规范要求的验证命令，应先补齐命令和 CI，而不是声称“无法运行”后交付。

## 6. 完成与交付

任务完成前必须确认：

1. 实现、测试、配置、文档和架构图保持一致。
2. 旧实现、临时文件、失效依赖和迁移桥已删除。
3. 全部适用门禁通过且无 warning、skip、flaky 或未解释失败。
4. 最终报告说明结果、关键决策、修改文件、执行的验证及任何尚需项目 owner 提供的外部输入。

不把计划、部分结果或“理论可行”描述为完成。
